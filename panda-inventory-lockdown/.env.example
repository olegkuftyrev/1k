# Copy to .env and adjust for your environment.
# Local dev uses the bundled docker-compose Postgres (see docker-compose.yml).
DATABASE_URL="postgresql://panda:panda@localhost:5434/panda?schema=public"
